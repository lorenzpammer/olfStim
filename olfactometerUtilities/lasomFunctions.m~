function lasomFunctions(instruction)
%
% instruction - can be 
% - 'checkConnection' -
%   - 'connect', which establishes 
% lorenzpammer July, 2012

%% extablish connection to LASOM

if strcmp(instruction,'connect')
lasomH = actxcontrol('LASOMX.LASOMXCtrl.1'); 
invoke(h2, 'DevOpen', 0, 1);
invoke(h2, 'GetLastError');
invoke(h2, 'GetID');

end